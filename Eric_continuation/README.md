## Eric's continuation

Continuation work: `famine_model.py`, `GOOD1_nonfixed_death_rates_Bayesian_bundle/`, `Wins/`, archived snapshots, etc. See `SOURCE_NOTE.txt`.

**Git branch:** `Eric's-continuation` (Git does not allow a space in `Eric's continuation`).
